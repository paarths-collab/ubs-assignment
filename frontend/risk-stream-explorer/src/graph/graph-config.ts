/** Debounce for filter recomputation, in milliseconds. */
export const FILTER_DEBOUNCE_MS = 150;

/** Debounce for search suggestions, in milliseconds — snappier than filters since it's typed character-by-character. */
export const SEARCH_DEBOUNCE_MS = 60;
